platform :ios, '15.0'
target 'BookshelfScanner' do
  use_frameworks!
  
  # RevenueCat for subscriptions
  pod 'RevenueCat'
  
  # Firebase (existing)
  pod 'Firebase/Core'
  pod 'Firebase/Auth' 
  pod 'Firebase/Firestore'
  pod 'Firebase/Analytics'
end
